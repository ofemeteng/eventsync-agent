modules = ["python-3.12"]
run = "uvicorn server:app --host 0.0.0.0 --port 80"
disableGuessImports = true


[nix]
channel = "stable-24_05"

[deployment]
run = ["uvicorn server:app --host 0.0.0.0 --port 80"]

[[ports]]
localPort = 80
